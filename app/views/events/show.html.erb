<%= render "shared/nav" %>

<% @event.event_instances&.each_slice(3) do |instances| %>
  <div class="row">
    <% instances.each do |event| %>
    <div class="col s12 m4">
      <div class="card">
        <div class="card-image">
          <img src="<%= event.image_url %>">
          <span class="card-title"><%= event.name %></span>
        </div>
        <div class="card-content">
          <p><%= event.description %></p>
        </div>
        <div class="card-action">
          <% if event.event_instances.count == 1 %>
            <a href="/events/<%= event.event_instances.last.id %>/register">See all</a>
          <% else %>
            <a href="/events/<%= event.id %>">Register</a>
          <% end %>
        </div>
      </div>
    </div>
    <% end %>
  </div>
<% end %>
